apiVersion: v1
kind: Service
metadata:
  name: {{ include "certificate-tools.fullname" . }}
  labels:
    {{- include "certificate-tools.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "certificate-tools.selectorLabels" . | nindent 4 }}
